<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cutoff Predictor</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f4f4f4;
    }
    .container {
      max-width: 600px;
      margin: 50px auto;
      background-color: #fff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    h2 {
      margin-top: 0;
    }
    .form-group {
      margin-bottom: 20px;
    }
    label {
      display: block;
      font-weight: bold;
      margin-bottom: 5px;
    }
    select {
      width: 100%;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }
    button {
      background-color: #007bff;
      color: #fff;
      border: none;
      border-radius: 5px;
      padding: 10px 20px;
      cursor: pointer;
    }
    button:hover {
      background-color: #0056b3;
    }


    /* Navbar container */
.navbar {
  background-color: #333; /* Background color */
  padding: 10px 20px; /* Padding for the navbar */
  display: flex; /* Make navbar items display in a row */
  justify-content: space-between; /* Distribute items evenly */
  align-items: center; /* Vertically center items */
}

/* Logo */
.logo a {
  color: #fff; /* Text color */
  text-decoration: none; /* Remove underline */
  font-size: 24px; /* Font size */
  font-weight: bold; /* Bold text */
}

/* Navbar links */
.nav-links {
  list-style: none; /* Remove default list styles */
  margin: 0; /* Remove default margin */
  padding: 0; /* Remove default padding */
}

.nav-links li {
  display: inline; /* Display list items horizontally */
  margin-right: 20px; /* Spacing between list items */
}

.nav-links li a {
  color: #fff; /* Text color */
  text-decoration: none; /* Remove underline */
  font-size: 18px; /* Font size */
}

/* Hover effect for links */
.nav-links li a:hover {
  text-decoration: underline; /* Underline on hover */
}


  </style>
</head>
<body>

  <nav class=" navbar ">
    <div class="logo"><a href="#">CCMS</a></div>
    <ul class="nav-links">
        <li><a href="/">Home</a></li>
        <li><a href="/tools">Tools</a></li>
        <li><a href="/cutoff">Cutoff</a></li>
        <li><a href="/login">Login</a></li>
    </ul>
</nav>

  <div class="container">
    <h2>College Cutoff</h2>
    <form action="/cutoff" method="post">
      <div class="form-group">
        <label for="iit">Select IIT:</label>
        <select name="iit" id="iit">
            <option>Select</option>
            <% for(let i=0; i < data.length; i++) { %>
              <!-- add onclick on this div -->
              <option value="<%= data[i].institute_code %>" data-name="<%= data[i].institute_name %>">
                <%= data[i].institute_name %>
              </option>
              
              <% } %>
        </select>       <!-- Add options for other IITs -->
        </select>
      </div>
      <div class="form-group">
        <label for="category">Select Category:</label>
        <select name="category" id="category">
          <option value="OPEN">General</option>
          <option value="OBC_NCL">OBC-NCL</option>
          <option value="SC">SC</option>
          <option value="ST">ST</option>
          <!-- Add options for other categories -->
        </select>
      </div>
      <div class="form-group">
        <label for="year">Select Year:</label>
        <select name="year" id="year">
          <option value="2023">2023</option>
          <option value="2022">2022</option>
          <option value="2021">2021</option>
          <option value="2020">2020</option>
          <option value="2019">2019</option>
          <!-- Add options for other categories -->
        </select>
      </div>
      <div class="form-group">
        <label for="gender">Select Gender:</label>
        <select name="gender" id="gender">
          <option value="Gender_Neutral">Gender-Neutral</option>
          <option value="Female_Only">Female-Only</option>
          <!-- Add options for other genders -->
        </select>
      </div>
      <button type="submit">Get Cutoff</button>
    </form>
  </div>
</body>
<script>
  const handleSubmit=(id)=>{
    console.log(id);
  }
</script>
</html>
